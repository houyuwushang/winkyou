# Legacy Execution Baseline Snapshot

This directory is for historical reference only.

## Source of Truth

The frozen legacy baseline is preserved by git tag:

- `legacy-ice-turn-baseline-2026-04-15`

To inspect the original snapshot directly:

```bash
git show legacy-ice-turn-baseline-2026-04-15:docs/EXECUTION-BASELINE.md
```

## Why This File Exists

The active path `docs/EXECUTION-BASELINE.md` has been converted into a clean legacy notice so that the repository has only one active architecture baseline.

This file preserves the historical pointer without restoring the old baseline to an active role.
