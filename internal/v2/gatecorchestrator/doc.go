// Package gatecorchestrator is the sole Gate C1b composition point. It joins
// an already-authorized Gate C product artifact, one bounded OOB stream, the
// Gate B executor, a production transport lease, and memory-only WireGuard.
// It never grants field-network or operating-system TUN authority.
package gatecorchestrator
