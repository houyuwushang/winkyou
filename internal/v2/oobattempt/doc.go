// Package oobattempt owns the strict, test-only Gate A artifact profile.
//
// The package performs only deterministic parsing, encoding, fingerprinting,
// and Noise-prologue construction. It has no network, filesystem, process,
// governor, probe, CLI, or runtime capability.
package oobattempt
