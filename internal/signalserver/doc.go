// Package signalserver provides a bounded, in-memory, test-only observation
// exchange point for two explicitly coordinated operators. It is not the v2
// production signaling or pairing protocol and must never carry secrets.
package signalserver
